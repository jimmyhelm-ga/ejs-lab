<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menu</title>
</head>
<body>
    <h1>Full Menu</h1>
  
    <!-- Render items under separate headers based on category -->
    <h2>Mains</h2>
    <ul>
      <% menu.filter(item => item.category === 'mains').forEach(item => { %>
        <li>
          <h3><%= item.name %></h3>
          <p>Price: $<%= item.price %></p>
          <p>Rating: <%= item.rating %></p>
          <p><%= item.details %></p>
        </li>
      <% }); %>
    </ul>
    
    <h2>Desserts</h2>
    <ul>
      <% menu.filter(item => item.category === 'desserts').forEach(item => { %>
        <li>
          <h3><%= item.name %></h3>
          <p>Price: $<%= item.price %></p>
          <p>Rating: <%= item.rating %></p>
          <p><%= item.details %></p>
        </li>
      <% }); %>
    </ul>
    
    <h2>Sides</h2>
    <ul>
      <% menu.filter(item => item.category === 'sides').forEach(item => { %>
        <li>
          <h3><%= item.name %></h3>
          <p>Price: $<%= item.price %></p>
          <p>Rating: <%= item.rating %></p>
          <p><%= item.details %></p>
        </li>
      <% }); %>
    </ul>  //I was strugging with the forEach method so chat GPT helped. I understand it now I believe. 
</body>
</html>